# SHARED ENGINE — fires automatically on every pull request; used by both
# path 1 (demo walkthrough) and path 2 (finding-driven/Jira-tracked). You do
# not normally run this by hand. See .github/workflows/README.md.
#
# Scan every pull request, and let the Sonar quality gate decide whether it can
# merge.
#
# This is the first and last arrow of the restated flow: it starts the loop, and
# the re-scan triggered by a remediation push is what closes it.
#
# THE JOB'S OWN CONCLUSION IS THE REQUIRED STATUS CHECK. Nothing here posts a
# separate commit status, because a second status is a second thing that can
# disagree with the first. Make the `gate` job's check required on main (the
# check context is the JOB name, so renaming this workflow's display name —
# it carries a presentation number — never touches branch protection).
#
# Fork pull requests are refused, not silently skipped. GitHub withholds
# repository secrets from a `pull_request` run on a fork, so SONAR_TOKEN is
# simply absent and the scan cannot run at all — the failure is at step 1, not
# at the push. `pull_request_target` is deliberately NOT used to work around
# this: it runs in the base repo's context with secrets and a write token
# against contributor-controlled code.
name: 02 - scan, gate & settle

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      pr:
        description: >-
          PR number to scan manually. Ignored on a real pull_request trigger —
          this only feeds the resolve job when there is no pull_request event
          to read from.
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write

# A remediation push re-triggers this workflow while the previous run may still
# be polling. Superseding is correct here: only the newest head matters.
concurrency:
  group: sonar-pr-scan-${{ github.event.pull_request.number || inputs.pr }}
  cancel-in-progress: true

jobs:
  # Normalizes "which PR, on what branches" for both triggers this workflow
  # accepts. On pull_request, github.event.pull_request already has every
  # field; on workflow_dispatch there is no PR event payload at all, so this
  # fetches the same fields by number instead. Keeping this as one job (rather
  # than inlining a fallback in every step below) means the pull_request path
  # is byte-for-byte what it was before manual dispatch existed.
  resolve:
    name: resolve pr context
    runs-on: ubuntu-latest
    outputs:
      number: ${{ steps.pr.outputs.number }}
      head_ref: ${{ steps.pr.outputs.head_ref }}
      base_ref: ${{ steps.pr.outputs.base_ref }}
      head_repo: ${{ steps.pr.outputs.head_repo }}
      html_url: ${{ steps.pr.outputs.html_url }}
      node_id: ${{ steps.pr.outputs.node_id }}
    steps:
      - id: pr
        name: Resolve PR context
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_PR_NUMBER: ${{ github.event.pull_request.number }}
          EVENT_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          EVENT_BASE_REF: ${{ github.event.pull_request.base.ref }}
          EVENT_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          EVENT_HTML_URL: ${{ github.event.pull_request.html_url }}
          EVENT_NODE_ID: ${{ github.event.pull_request.node_id }}
          MANUAL_PR: ${{ inputs.pr }}
        run: |
          if [ -n "$EVENT_PR_NUMBER" ]; then
            echo "number=$EVENT_PR_NUMBER" >> "$GITHUB_OUTPUT"
            echo "head_ref=$EVENT_HEAD_REF" >> "$GITHUB_OUTPUT"
            echo "base_ref=$EVENT_BASE_REF" >> "$GITHUB_OUTPUT"
            echo "head_repo=$EVENT_HEAD_REPO" >> "$GITHUB_OUTPUT"
            echo "html_url=$EVENT_HTML_URL" >> "$GITHUB_OUTPUT"
            echo "node_id=$EVENT_NODE_ID" >> "$GITHUB_OUTPUT"
          else
            gh api "repos/${{ github.repository }}/pulls/$MANUAL_PR" > pr.json
            echo "number=$(jq -r .number pr.json)" >> "$GITHUB_OUTPUT"
            echo "head_ref=$(jq -r .head.ref pr.json)" >> "$GITHUB_OUTPUT"
            echo "base_ref=$(jq -r .base.ref pr.json)" >> "$GITHUB_OUTPUT"
            echo "head_repo=$(jq -r .head.repo.full_name pr.json)" >> "$GITHUB_OUTPUT"
            echo "html_url=$(jq -r .html_url pr.json)" >> "$GITHUB_OUTPUT"
            echo "node_id=$(jq -r .node_id pr.json)" >> "$GITHUB_OUTPUT"
          fi

  gate:
    name: gate
    needs: resolve
    runs-on: ubuntu-latest
    # The light container shape from docs/decisions/ci-container.md. The scan
    # needs no JRE of its own: sonarqube-scan-action downloads a Scanner CLI
    # that carries an embedded Java 21.
    container: node:24-bookworm

    steps:
      # FIRST, before any checkout or secret use. A fork PR must end red.
      - name: Refuse fork pull requests
        if: needs.resolve.outputs.head_repo != github.repository
        run: |
          echo "::error title=Fork pull requests are out of scope::GitHub does not pass repository secrets to a pull_request run from a fork, so SONAR_TOKEN is absent and the scan cannot run. This check fails deliberately rather than reporting a green gate it never earned."
          exit 1

      - uses: actions/checkout@v5
        with:
          # Sonar needs real history for new-code attribution and blame.
          fetch-depth: 0
          # EXPLICIT, and it has to be. On a `pull_request` event a bare
          # checkout already lands on the merge ref, so this line is a harmless
          # restatement there. On a MANUAL dispatch there is no pull_request
          # event to inherit from, and a bare checkout takes the DEFAULT
          # BRANCH — so the scan analysed `main` and Sonar published the result
          # as the PR's own analysis.
          #
          # Proven, not inferred: run 36091767072 (2026-09-25) dispatched with
          # pr=10 logged `git checkout --progress --force -B main
          # refs/remotes/origin/main`, and PR #10's 32 planted findings stayed
          # CLOSED afterwards because clean main has none. That is the exact
          # failure this repository is built against — a gate verdict about a
          # tree the pull request does not contain — and it runs on the path
          # that produces the REQUIRED check, so a manual re-run could publish
          # a green gate computed from clean main onto a PR that is red.
          #
          # The merge ref is what a pull-request scan is supposed to evaluate,
          # and it is what the automatic path has always evaluated.
          ref: refs/pull/${{ needs.resolve.outputs.number }}/merge

      # Coverage has to be measured before the scan, not after: the scanner
      # reads lcov off disk. This also means a red test suite fails the gate
      # before Sonar is ever called — deliberate, since a PR whose tests do not
      # pass has nothing worth scanning.
      - name: Install dependencies
        run: npm ci

      # The gate's own reporting path. Deliberately not part of `npm test`:
      # that runs on the raw PR head in remediate.yml, where this file does not
      # exist. Here the merge ref carries main's scripts, so it is both safe and
      # the only place the reporter is exercised before it reports.
      - name: Test the gate's reporting path
        run: npm run test:scripts

      - name: Test with coverage
        run: npm run test:coverage

      - name: Make lcov paths repo-root relative
        run: |
          node .github/scripts/normalize-lcov.mjs api/coverage/lcov.info api
          node .github/scripts/normalize-lcov.mjs web/coverage/web/lcov.info web

      - name: Scan the pull request
        uses: SonarSource/sonarqube-scan-action@v7
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          args: >
            -Dsonar.pullrequest.key=${{ needs.resolve.outputs.number }}
            -Dsonar.pullrequest.branch=${{ needs.resolve.outputs.head_ref }}
            -Dsonar.pullrequest.base=${{ needs.resolve.outputs.base_ref }}

      # continue-on-error so a red gate still gets its comment posted below.
      # The verdict is re-asserted in the final step.
      - name: Wait for the gate and render the findings
        id: gate
        continue-on-error: true
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: node .github/scripts/pr-gate.mjs .scannerwork/report-task.txt "${{ needs.resolve.outputs.number }}"

      - name: Comment on the pull request
        if: always() && steps.gate.outcome != 'skipped'
        uses: actions/github-script@v8
        env:
          PR_NUMBER: ${{ needs.resolve.outputs.number }}
        with:
          script: |
            const fs = require('fs');
            if (!fs.existsSync('pr-comment.md')) {
              core.warning('no pr-comment.md — the gate step failed before rendering one');
              return;
            }
            const body = fs.readFileSync('pr-comment.md', 'utf8');
            const marker = '<!-- sonar-pr-gate -->';
            const { owner, repo } = context.repo;
            const issue_number = Number(process.env.PR_NUMBER);
            const existing = await github.paginate(
              github.rest.issues.listComments, { owner, repo, issue_number }
            );
            // Rewrite in place. One scan, one comment — a fresh comment per
            // push would bury the PR under a scan log.
            const mine = existing.find((c) => c.body && c.body.includes(marker));
            if (mine) {
              await github.rest.issues.updateComment({ owner, repo, comment_id: mine.id, body });
            } else {
              await github.rest.issues.createComment({ owner, repo, issue_number, body });
            }

      - name: Enforce the verdict
        if: steps.gate.outcome == 'failure'
        run: |
          echo "::error::The Sonar quality gate did not pass. This pull request stays blocked."
          exit 1

  # Deliberately a SECOND job, and deliberately not called `gate`.
  #
  # `gate` is the required status context in branch protection. Anything added
  # to it can block a merge, and settle must never be able to: it reports a
  # terminal state and optionally flags auto-merge intent, and a bug in it
  # blocking an otherwise-green PR would be a worse failure than not running.
  # `needs: gate` with `if: always()` means it observes the gate rather than
  # participating in it -- it runs on a red gate too, which is the case whose
  # message the whole settle stage exists to produce.
  #
  # This is also why settle is not a step in remediate.yml: remediation pushes,
  # the push re-triggers THIS workflow, and only here is the gate the fixed
  # code's gate rather than the one from before the fix.
  #
  # Factored into _settle-notify.yml so the auto-continue watcher
  # (auto-continue-watch.yml) can call the exact same settle+notify+Jira-outcome
  # logic a PR-triggered scan runs here, instead of a second implementation.
  settle:
    name: settle
    needs: [resolve, gate]
    if: always() && needs.resolve.outputs.head_repo == github.repository
    # A called reusable workflow's permissions are capped by the caller's —
    # the top-level `permissions:` above is read-only for contents, but
    # _settle-notify.yml pushes to automation-state and comments on the PR.
    permissions:
      contents: write
      pull-requests: write
    uses: ./.github/workflows/_settle-notify.yml
    with:
      sonar_project_key: phix_sonar-sandbox-app
      pr: ${{ needs.resolve.outputs.number }}
      pr_url: ${{ needs.resolve.outputs.html_url }}
      pr_node_id: ${{ needs.resolve.outputs.node_id }}
    secrets: inherit

  # DOES SONAR ACTUALLY REPORT WHAT THE CATALOGUE PLANTED?
  #
  # This is the demo's premise — "a PR carrying N planted smells for the pipeline
  # to remediate" — and until now nothing asserted it. The reset proved the
  # branch carried the smells with `npm run smells:verify`, which runs
  # eslint-plugin-sonarjs, and that pre-flight says so itself: "This is a
  # pre-flight. The authoritative gate is a real Sonar scan." It can only see 27
  # of the 32 catalogue rules and reports "5 skipped: rule not implemented by
  # the local plugin". So "Sonar-ready" was assumed by the one stage that had no
  # way to know.
  #
  # A SEPARATE JOB, on the advice of Jev (TypeSafe System One, choice
  # confidence 0.97 over adding it to `gate`, a manual workflow, and polling
  # inside the reset). Three reasons it is its own job and not a `gate` step:
  #
  #   1. `gate` MEANS the quality gate. Branch protection requires it, so
  #      anything added there can block a merge — and "Sonar reported everything
  #      we planted" is a fact about the test oracle, not about the change under
  #      review. The same argument the `settle` job above already makes.
  #   2. It belongs HERE, immediately after the analysis that produces the
  #      evidence. No polling: `needs: gate` means the scan has finished, so the
  #      findings exist by the time this starts.
  #   3. Its own check name makes the claim legible on the PR: `oracle` green
  #      means the demo is genuinely Sonar-ready.
  #
  # `needs: gate` WITHOUT `always()`: on a red gate there is still an analysis to
  # check, so this runs regardless — a red gate is the demo's NORMAL state and
  # says nothing about whether the oracle is intact.
  oracle:
    name: oracle
    needs: [resolve, gate]
    if: always() && needs.resolve.outputs.head_repo == github.repository
    runs-on: ubuntu-latest
    container: node:24-bookworm
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
        with:
          # The merge ref, for the same reason `gate` uses it — and it is where
          # the planted catalogue lives. A bare checkout would read the DEFAULT
          # BRANCH's empty catalogue and pass vacuously.
          fetch-depth: 0
          ref: refs/pull/${{ needs.resolve.outputs.number }}/merge

      # main's catalogue is EMPTY BY DESIGN (main is the clean baseline), so on a
      # group branch there is nothing planted and nothing to assert. Skipping is
      # reported rather than silent: an `oracle` check that went green on a
      # branch carrying no catalogue would be exactly the kind of green this
      # pipeline exists to refuse.
      - name: How much does this branch plant?
        id: planted
        run: |
          n=$(node -e "console.log((require('./smells/catalogue.json').smells || []).length)")
          echo "planted=$n" >> "$GITHUB_OUTPUT"
          if [ "$n" = "0" ]; then
            echo "::notice title=No catalogue on this branch::this branch plants nothing (main's catalogue is empty by design), so there is no oracle to check. The job is green because there is no claim to verify, not because a claim held."
          else
            echo "this branch plants $n smell(s) — asserting Sonar reports every rule key."
          fi

      - name: Check out the automation repo
        if: steps.planted.outputs.planted != '0'
        uses: actions/checkout@v5
        with:
          repository: phix/sonar-remediation-automation
          path: .automation

      # The asserter wants Sonar's RAW api/issues/search response, not the
      # normalised findings the fix engine reads, so this fetches it directly.
      # One page is enough for a catalogue this size; past 500 findings this
      # would silently check a subset, and `assert-catalogue-coverage.mjs`
      # accepts an ARRAY of pages precisely so that pagination is the fix when
      # the demo grows.
      - name: Fetch what Sonar actually reported
        if: steps.planted.outputs.planted != '0'
        env:
          SONAR_PROJECT_KEY: phix_sonar-sandbox-app
          PR: ${{ needs.resolve.outputs.number }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          node -e "
            const fs = require('fs');
            const url = 'https://sonarcloud.io/api/issues/search?' + new URLSearchParams({
              componentKeys: process.env.SONAR_PROJECT_KEY,
              pullRequest: process.env.PR,
              statuses: 'OPEN,CONFIRMED,REOPENED',
              ps: '500'
            });
            fetch(url, { headers: { authorization: 'Basic ' + Buffer.from(process.env.SONAR_TOKEN + ':').toString('base64') } })
              .then((r) => { if (!r.ok) throw new Error('GET ' + url + ' -> HTTP ' + r.status); return r.text(); })
              .then((t) => fs.writeFileSync('issues.json', t))
              .catch((e) => { console.error(e.message); process.exit(1); });
          "

      - name: Diff reported rule keys against the catalogue
        id: assert
        if: steps.planted.outputs.planted != '0'
        # Reported, then enforced in the step after the comment — so a MISS is
        # visible on the PR even though the job ultimately goes red.
        continue-on-error: true
        run: |
          node .automation/scripts/assert-catalogue-coverage.mjs \
            --issues issues.json --catalogue smells/catalogue.json --report oracle.md

      # THE MARKER IS WHAT MAKES THIS AN UPSERT rather than a fresh comment on
      # every scan. Every other stage's report carries its marker as the first
      # line of the file, written by the generator that produces it; this report
      # is written by `assert-catalogue-coverage.mjs`, which knows nothing about
      # markers, so it is prepended here. Verified the hard way: without this the
      # comment posts fine and silently never upserts.
      #
      # `always()` because the asserter writes the report and THEN exits non-zero
      # on a MISS — the failure case is exactly when the comment matters most.
      - name: Mark the report so it upserts instead of piling up
        if: always() && steps.planted.outputs.planted != '0'
        run: |
          printf '<!-- sonar-oracle -->\n\n' | cat - oracle.md > oracle.marked.md
          mv oracle.marked.md oracle.md
          head -1 oracle.md

      - uses: ./.github/actions/upsert-pr-comment
        if: always() && steps.planted.outputs.planted != '0'
        with:
          file: oracle.md
          marker: "<!-- sonar-oracle -->"
          pr: ${{ needs.resolve.outputs.number }}

      - name: Fail when a planted rule went unreported
        if: steps.assert.outcome == 'failure'
        run: |
          echo "::error title=Sonar does not report the planted catalogue::A planted smell that Sonar never reports is a defect in the test oracle, not a passing demo. See the comment on this PR for which rule key went missing. 05 - remediate a PR would find nothing to fix for it, and the demo would look like a pipeline with nothing to do."
          exit 1
